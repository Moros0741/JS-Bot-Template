// CONFIG VARIABLES. You can use a .env if you wish. just chance the index.js and deployCommands.js file to support .env files.
// Make sure this is in your .gitignore so its not accidentally pushed to github. 

export default config = {
	clientId: "",
	guildId: "",
	token: "",
	database_srv: "",
	owners: ["", "", ""],
	password: "", // Sudo Login Password for Bot.
};